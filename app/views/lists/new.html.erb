<div class="container new-item-form">
  <h1>Create a New List</h1>
  <%= simple_form_for(@list) do |f| %>
    <%= f.input :name %>
    <%= f.submit "Submit New List", class: "body-button" %>
  <% end %>
</div>
